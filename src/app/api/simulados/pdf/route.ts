// TODO (Semana 6): Endpoint de geração de PDF
// POST /api/simulados/pdf
// Responsável por: receber questões, gerar PDF com Puppeteer, retornar URL
export async function POST() {
  return Response.json({ message: "TODO: gerar PDF" });
}
