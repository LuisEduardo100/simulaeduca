// TODO (Semana 5): CRUD de simulados
// GET /api/simulados — listar simulados do usuário
// POST /api/simulados — criar novo simulado (rascunho)
export async function GET() {
  return Response.json({ message: "TODO: listar simulados" });
}

export async function POST() {
  return Response.json({ message: "TODO: criar simulado" });
}
