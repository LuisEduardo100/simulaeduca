// TODO (Semana 4-5): Endpoint de geração de simulado via IA
// POST /api/simulados/gerar
// Responsável por: validar créditos, acionar agente gerador, retornar questões
export async function POST() {
  return Response.json({ message: "TODO: gerar simulado com IA" });
}
