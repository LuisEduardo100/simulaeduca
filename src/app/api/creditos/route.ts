// TODO (Semana 7): Endpoint de gerenciamento de créditos
// GET /api/creditos — saldo e histórico do usuário
export async function GET() {
  return Response.json({ message: "TODO: saldo de créditos" });
}
